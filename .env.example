# Optional private Vercel Blob credentials; browsing works without these.
BLOB_READ_WRITE_TOKEN=
SIGNUP_HASH_SECRET=
# Default localhost dev port; override origins too if changing it.
PORT=8767
HEADSTART_ALLOWED_ORIGINS=http://127.0.0.1:8767,http://localhost:8767
