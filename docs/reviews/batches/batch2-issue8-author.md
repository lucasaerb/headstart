# Issue #8 author report — local brief and versioned shortlist

Author: batch2_brief. Branch: feat/backlog-batches, batch 2 working revision after independently reviewed batch 1 (`9a85fde`). Author ready; independent batch review remains required. Issue #7 detail author supplied the Save integration interface during this batch; this is dependency coordination, not a substitute for its independent acceptance.

Implemented `brief-store.js`, `brief-collection.js`, `brief-collection.css`, plus tests. The existing C4 hero is untouched. A compact **My project** action opens the existing dialog visual language. The idea field is immediately available; all other editable constraints stay under an optional disclosure. Each constraint distinguishes explicit input from an inferred, editable assumption. Search text can seed the idea as an assumption only when opening the editor; it is never silently persisted. Eleven constraint dimensions, monotonic brief revision and previous revision metadata round trip locally. The schema is a browser-local planning envelope, not a platform reuse handoff.

Project Save and scoped component Save retain source commit/version identity and public comparison metadata. A separate 24-version shortlist leaves the three-game remix bag intact; up to four entries can be compared for runtime, rights scope, readiness, style, known dependencies and the current runtime goal. Unknowns remain visible. Reload preserves saved versions, changed catalog versions do not overwrite them, absent IDs are marked unavailable only after a complete bounded catalog scan. Failed or incomplete availability checks remain unknown. No account sync, source bytes, verified identity or automatic agent handoff is claimed.

Storage errors retain the current working state and offer export. Malformed/unsupported old data stays untouched and can be exported before explicit reset. Reset affects only this feature; demo email preference and remix bag remain intact. All user/catalog text is inserted with textContent. Native dialog provides keyboard trapping, Escape and return focus. Failed asynchronous availability checks cannot replace a later Brief/Compare tab.

## Validation performed

- `node --test tools/site/brief-store.test.mjs`: **4 PASS**, including explicit/inferred revision round trips, invalid constraints, two immutable component versions, stale/unavailable/unknown states, older malformed data, denied storage and missing source revisions.
- `HEADSTART_CHROME_CHANNEL=chrome node tests/brief-collections-ui.mjs`: **PASS** in real Chrome, desktop 1440×1000 and mobile 390×844. Tested project and component Save, editing distinct constraints, save/reload, two-item comparison, metadata export, stale version retained, removed reference unavailable, reset, malformed recovery and storage-disabled working state. No page JS exceptions or horizontal page/dialog overflow.
- Evidence: `docs/reviews/game-brief-collections/evidence/{desktop,mobile}-{brief-initial,brief-edit,saved,compare,stale,unavailable,recovery,storage-disabled}.png`.
- Author visually inspected desktop editor and mobile comparison; final visual verdict belongs to separate reviewer. Selected reference: C1 `C-one-good-match.png`, SHA256 `17253dec26774f5169adb0a5f060cde0ff85c31b3f0a27a1119c61b33cef5dad`; retained C4/C4b/C2/C3 baselines and digests are in `docs/design/C4-FINAL-STORYBOARD.md`. Optional disclosure keeps the initial editor compact.

Limits: browser-local state has no account backup; losing/resetting browser storage loses unexported planning data. Unknown source revisions remain unversioned references. Comparison reports recorded metadata and does not prove target compatibility or assembled integration. Previous revisions are retained as metadata (last 100), not a source-code history. Independent review pending.

Final batch regression uses explicit 2048 project / 2048-tile component IDs after the 30-component seed. It exposed long scoped metadata overflowing the mobile library behind the dialog; foundation fixed wrapping on `.reviewed-system`, then this suite passed again. The initial collapsed Brief screenshots are retained separately from expanded editing captures. Root `test:brief` and CI run the browser suite and collect artifacts.
