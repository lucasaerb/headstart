# Batch 1 / issue #4 author evidence

Author: `/root/batch1_data`. Independent reviewer: pending batch reviewer. Verdict: authored, **not independently accepted**.

Implemented `services/catalog/`: local SQLite metadata/evidence repository, explicit research-only view, immutable canonical entity versions, contract/relationship validation and registered digest verification at write boundary, derived published component view requiring published source parent, tombstones retaining audit/notices, independent dated demo observations, transaction rollback, and safe digest-addressed evidence reads/display rights boundary. Local operations and Postgres migration direction are in `services/catalog/README.md`.

Checks: `/tmp/headstart-contracts-venv/bin/python -m unittest discover -s services/catalog/tests -v`: 11 passed on 2026-09-10. Real import twice preserves exact table exports: current source contains **76** references, none automatically published. Tests reject duplicate provider/subproject import atomically; preserve changed research revisions/provenance; reject unresolved rights, absent evidence bytes and immutable changes; preserve source visibility after demo failure; hide tombstoned versions and parent projects; detect digest tampering, path traversal and symlinks; enforce display approval; restore backup; roll back nested import transaction. Synthetic records in unit tests are explicitly synthetic and never seeded into application storage.

No UI changed: desktop/mobile screenshots and design comparison N/A. No cloud provisioning or source execution. SQLite is local only and does not establish production Postgres or Vercel persistence. Database mutation interfaces are trusted curator/local-process operations and are not exposed as anonymous HTTP mutations. Complete evidence bytes require digest verification; curator assertions still require human review, rather than deriving rights from open-source labels.

Dependent contract #3 independent review is pending batch review; canonical write acceptance remains dependent on that result. #5 builder fetched pinned 2048 `js/tile.js` and `LICENSE.txt`; this author statically inspected both and provided `services/catalog/seed.py`, a digest-pinned, idempotent metadata seed restricted to these two files. It is source-reviewed only, has no assets, and claims no isolation/integration success. Its separate fixture README preserves exact source URLs and full MIT notices. Tests reject its unsupported integration-tested promotion. Independent batch review is still required. File digests and final reviewer verdict to be captured after batch freeze.

## Independent review fixes: execution evidence and graph insertion

Addressed reviewer P1 findings in contracts and persistence: bound verification to immutable recipe/target and exact scope; check structured digest-verified execution reports and their nonempty log blobs; reject relabeled source bytes as execution evidence. Added atomic `put_records` for legitimate component/dependency cycles with complete final-graph validation and ownership checks. Optional represented files now require rights coverage too.

Recheck: `/tmp/headstart-contracts-venv/bin/python -m unittest discover -s services/catalog/tests -q` — **25 tests passed**, including concurrently added API tests. `/tmp/headstart-contracts-venv/bin/python -m unittest discover -s contracts/tests -q` — **13 passed**. Positive execution records are explicitly synthetic unit fixtures; the real 2048 seed remains source-reviewed only. Independent reviewer notified for re-review; author does not self-approve.
