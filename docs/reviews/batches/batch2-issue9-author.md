# Batch 2 / issue #9 author evidence

Author: `/root/batch1_data`. Independent review: pending batch 2 reviewer. Prerequisite batch 1 #4 independently passed at `9a85fde`, per coordinator; current work consumes its design boundaries without changing catalog store/contracts.

Implemented `services/intake/`: durable SQLite normalize/snapshot/inspect jobs; request-hash idempotence; analyzer/tree-digest cache; bounded backoff, three-attempt dead-letter and explicit manual retry. Public GitHub/raw HTTPS only; DNS answers must be global, socket connects to checked numeric address with original TLS hostname, redirects/compression rejected, no archive extraction. Selected paths bounded at100 files,256KiB/file,2MiB total. Traversal/credential filenames, binary and detected secret content are excluded. No upstream code executes.

Static JS/TS literal analysis emits source digests/ranges, imports, missing/out-of-subset references, dynamic unknowns, assets/service hosts and dependency classifications. package.json and supported package-lock metadata are parsed; unsupported lock parsers are explicit unknowns with retained digests. No capability generation, complete-tree claim, rights clearance or integration success is inferred.

Validation: `python3 -m unittest discover -s services/intake/tests -v` — **8 passed**. Fixtures cover unsupported/private/metadata schemes and DNS responses, pinned DNS connection and redirect rejection, traversal/count/per-file/total-byte limits, archive/binary/secret exclusion, inert injection text, missing/dynamic imports, manifest classifications, retry restart/idempotence, dead-letter/manual retry and redacted errors. Read-only real public fetch succeeded for 2048 commit `478b6ec346e3787f589e4af751378d06ded4cbbc`, only `js/tile.js` and `LICENSE.txt`; two retained files, selected-tree digest `69b3426556ec664f321b015e553b2e6e5b2561d8737671527b83319f0564b366`. Report: `batch2-issue9-public-snapshot.json`. Source bytes unchanged from independently reviewed #4 scope. No code/install script was executed.

Screenshots/design comparison N/A: local source-intake infrastructure only, no UI. Known limits are documented: selected-file subset, GitHub only, literal-analysis approximation, unsupported lock parsers, pattern-based secret detection and trusted curator input. No production deployment. Final verdict awaits independent review; this is author evidence only.
