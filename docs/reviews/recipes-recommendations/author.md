# Recipe recommendations — author evidence

The local `/recipes.html` route is connected to the public metadata recommendation API, included in all three static allowlists, and linked from the homepage footer. The existing C4 hero is unchanged. The page uses its existing navy type, pale blue background, white form panels and blue actions. Original C1 guides the concise editable brief, explained recommendation and alternative. This secondary page shows three source-pinned planning recipes; simple palette studies are illustrative, not game screenshots. No recipe is represented as a tested combination.

Selected references: `HeadStart-Starter-Package/design-explorations/2026-09-simplicity/C-one-good-match.png` SHA256 17253dec26774f5169adb0a5f060cde0ff85c31b3f0a27a1119c61b33cef5dad; C4 `C4-living-world-landing.png` SHA256 81f0dd99b3891c0ecd33605544e99617c5eaf8a803baf737c4b1597e44f0a8f9; C2 `C2-browse-list.png` SHA256 bad898855647f1782220aa886e75247a8d5fd396bc2476ace09ae33176b97d6f. Author inspected actual desktop/mobile list and mobile handoff captures against these references; independent design acceptance remains the reviewer's decision.

`HEADSTART_CHROME_CHANNEL=chrome SCREENSHOT_DIR=/tmp/headstart-recipes-captures node tests/recipes-ui.mjs` passed with real local seeded source records and a synthetic verified session at 1440×1000 and 390×844. The test launches installed Chromium after clearing service configuration; its browser captures cover contrasting briefs, exact query-to-persisted-rationale continuity, recipe details, blocked whole-composition handoff, successful scoped Simplex handoff, explicit empty results and HTTP-error states. Representative captures are retained here; every scenario is also in the temporary capture directory.

The tested query is `camera`; persistent rationale is recomputed from the versioned brief alone, so transient search terms do not cause repeated stale-context rejection. Runtime and readiness hard filters remain explicit. Source/license scope freezes invalidate recipe context, including a frozen unselected component of the proposed recipe. Historical packets remain immutable.

Whole recipes containing unresolved external engine peers fail the existing protected handoff boundary. The UI explains this and permits a separately selected self-contained capability; its exact same informational recipe context survives in schema2 bag/packet. The executable adapter remains distinct and unsupported assembled recipes do not become executable. No rights boundary was relaxed for this UI test.

Commands: `.venv/bin/python -m unittest discover -s services/recommendations/tests -v` (11 PASS); `.venv/bin/python -m unittest discover -s tools/review -v` (12 PASS). The real Codex0.7 catalog check additionally prepared and retrieved a schema2 recipe bag and handoff automatically; see `../expert-review-skills/codex-client.json`.
