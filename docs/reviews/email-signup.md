# Email signup and demo lead capture review

2026-09-10. Author `/root/release_author`; independent reviewer `/root/release_reviewer`. **Local implementation verdict: PASS; production persistence verification pending.** Reviewed exact files in [digests.json](evidence/email-signup/digests.json) in the isolated `/tmp/headstart-email-signup` worktree.

Scope includes the optional homepage updates form plus the user's subsequent explicit requirement to save an email before the site's Play demo flow. This overrides the earlier anonymous in-site demo launch requirement only for that flow; upstream sites remain public. Capture does not establish verified identity, code-delivery authorization or outbound email delivery.

## Independent checks

Read API, client, owner CLI, build allowlist, pinned SDK types/source and all tests. Ran `node --test tools/signup/subscribe.test.mjs`: **11 passed**. Coverage includes malformed input/method/origin/type/size, honeypot, consent, private HMAC paths, atomic no-overwrite writes, concurrent deduplication and five-slot rate limiting, ambiguous writes requiring matching origin readback, separate demo/update consent records and retry recovery after partial purpose writes. SDK 2.8.0 supports `allowOverwrite:false` and `useCache:false`; duplicate handling uses an exact private readback rather than assuming an unsupported exception class.

Independent Chromium browser tests at 1440×1000, 390×844 and 320×844 exercised the optional form and demo modal using explicitly intercepted success/failure responses. These verify client behavior, not real storage. Homepage signup remains visible initially with no page overflow. Failure retains input and shows an honest error; success resets the input. Demo marketing consent defaults unchecked; failure keeps launch unavailable; successful response reveals the exact original external URL with `noopener noreferrer`. No email is written to localStorage. See [homepage observations](evidence/email-signup/checks.json) and [demo observations](evidence/email-signup/demo-checks.json).

Viewed independent screenshots [desktop signup](evidence/email-signup/signup-1440.png), [mobile signup](evidence/email-signup/signup-390.png), [narrow signup](evidence/email-signup/signup-320.png), [desktop demo](evidence/email-signup/demo-1440.png), [narrow demo](evidence/email-signup/demo-320.png). Controls and consent are legible and fit the selected C4 visual language. The new homepage panel partly overlays decorative companion/speech space on desktop; nonblocking tradeoff, with primary discovery controls and signup usable. The accessible native dialog retains a visible cancel control and focused email field. Author separately exercised Escape cancellation; late requests are guarded by attempt IDs so closed dialogs do not launch a game.

## Privacy and release boundaries

API stores normalized email, purpose, explicit consent version/text, timestamp and unverified/not-sent state privately. Demo-only and updates consent have separate deterministic records; later opt-in does not erase original evidence. No raw email/IP is used in a blob pathname; rate objects contain expiry only. No public list/export/delete route exists. Owner commands remain outside dist, export to new mode-0600 files, delete both purpose records and prune old rate buckets. No sender is implemented. Private credentials are server-only. Public build copies an explicit allowlist and excludes API source, package files, node_modules, build script and owner tooling.

Limits: this is a lead form, not authentication or an unbypassable security boundary around external demos. Email ownership remains unverified. Rate limits are per IP/hour and require periodic pruning; they are not comprehensive bot protection. Secret rotation requires preserving/migrating lookup paths. Real deployment must verify function routing, private write/readback/deduplication and synthetic-record deletion, and that server/package files are not downloadable. No production storage success is asserted by this local PASS.

## Production backend evidence review — PASS

Independently inspected the coordinator's actual verification script `/tmp/headstart-signup-live.mjs` and [sanitized results](evidence/email-signup/production-storage.json) for `https://headstart-anzyi4bge-lucasaerbs-projects.vercel.app`. This used real deployment HTTP requests via authenticated `vercel curl` and private SDK origin readbacks, not mocks. Assertions verify a demo-only record with no marketing record, subsequent explicit homepage consent, duplicate preserving initial record bytes, foreign-origin rejection, private Blob URL access denial, and no exposed package/build/dependency/environment/owner files or API source. A `finally` block deletes both synthetic records and asserts fresh private reads return null. The script keeps credentials external and saves no email in its result record.

Backend acceptance is **PASS** based on inspected execution evidence. The independent reviewer did not repeat production writes. Public-primary browser verification follows promotion; protected deployment verification does not establish the public alias is updated.
