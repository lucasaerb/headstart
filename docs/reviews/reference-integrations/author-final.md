# Issue17 final author evidence

Implementation source `2ea7a4d`, plugin0.7.0, exact local image `sha256:c7acddd94000df88bc95cef1c1519bca02865e6607ab9ba496a1f608410decd5`. This record reports author validation; independent acceptance remains separate.

All ten distinct source capabilities passed unchanged baseline and integrated target runs at1280×800 and390×844: forty successful browser phases. Twenty real broken runtime mutants (no-op and missing disposal per capability) failed their expected behavior/cleanup checks. Twenty-one host contract/guard tests passed. Exact target/source/recipe/brief/plugin/environment hashes, per-phase checks, raw logs, all forty captures and catalog-compatible execution attestations are retained in `final-0.7/`; `author-summary.json` is the acceptance hash index.

Source is Three.js commit `5c5a575bd8cc0cf440026ce8dcf6a77862067684`, engine0.186.0. Sources and complete local addon dependencies retain MIT and inline notices. Targets preserve their original renderer, camera, frame scheduler, keyboard explorer and flat-world topology; physics is explicitly none. The matrix tests scoped queries and geometry/generation/debug integration, not camera controllers, R3F, collision response or universal portability. Orbit and PointerLock remain untested; the documented ConvexGeometry and VertexTangentsHelper replacements were agreed before execution.

Visual author review inspected all desktop/mobile phase captures via the retained contact sheets and full mobile captures for the query overlay and parametric surface. Earlier visual findings were corrected: the baseline landmark no longer hides the explorer, the parametric surface is visibly curved from the existing camera, and Capsule/OBB overlay captures follow actual target keyboard/update movement into translated query regions. The narrow game viewport crops outer world edges; controls and the explorer remain visible. These diagnostic targets are separate from the unchanged C4 HeadStart site.

Actual behavior covers deterministic connected terrain, strict Capsule overlap/tangency semantics, rotated OBB intersection, rounded corner geometry/normals, analytic parametric positions/UVs/normals, construction-frame endpoints, transformed normal/tangent helpers and convex hull extremes/enclosure/volume. Every row exercises start/update/pause/reset/dispose, repeated mount cleanup, actual listener/frame removal and continued use of the original world after adapter teardown.

The explicit local promotion helper requires a distinct reviewer and exact row validation/attestation/recipe hashes, verifies all four successful phases and retained evidence bytes, and creates immutable source version2 records with matching target/recipe references. All ten actual execution attestations and their log blobs passed the catalog contract in a disposable database using an explicitly synthetic test reviewer label. This was a contract test, not a retained approval or publication. Original source-reviewed versions remain unchanged; controlled target metadata and assembled-redistribution rights remain candidate rather than receiving an invented blanket license. Normal catalog seeding does not silently promote these records.

Reproduce each row or all ten using `tools/integration/matrix/README.md`; use a fresh output directory and the recorded immutable image. Author output roots were `/tmp/headstart-matrix-release07` and `/tmp/headstart-matrix-release-negative07`. No target source was uploaded or merged, no production credentials were used, and no production catalog promotion occurred.
