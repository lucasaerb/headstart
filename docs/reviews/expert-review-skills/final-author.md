# Expert review skills — final candidate evidence

Candidate0.7.0 packages all twelve skills and both review helpers. The three new skills share the actual pinned integration plan/packet/brief context through `integration_review_context.py`; recipe context is informational and remains candidate, never an executable or tested composition. The helper checks selected record/version/source/file evidence, source parent/repository, record and bag digests, target file/mode digest and schema2 recommendation continuity. It cannot authenticate an author or replace current target inspection and independent validation review.

`codex-client.json` records a fresh actual Codex0.154.0 installation under a distinct disposable marketplace, all twelve enabled skills, public catalog lookup, exact detail, automatic ordinary bag/handoff, real website-prepared schema2 recipe context in automatic bag/handoff retrieval, revoked/offline denial, explicit snapshot fallback and0.3 rollback. `actual-skill-review.json` records a real read-only model turn invoking the three new skills, actual host source reads, all three image views and both packaged helpers on first-party synthetic fixtures. It reports source-located listener/duplicate-world findings, observed art regions versus editorial preference, and rejects FPS/speedup claims from the single previous CPU-only toy benchmark. No fixture or target bytes changed.

`final-client-summary.json` verifies every installed runtime/skill hash against the final45-file archive, SHA256 b8038b0edaaefb63221befe0fd360fab117ac10a076bbd5a4679e42edada4d32. The distinct test plugin/marketplace were removed afterward; the user's personal0.3 plugin remains. No credentials, actual private target or raw user captures were supplied to the model. Test workspace paths in transcripts refer to disposable synthetic projects and are no longer live links.

The earlier independent fixture evidence (`art-guide.md`, `code-and-performance-review.md`, actual before/after captures, `benchmark.json`, `raw.json`, `evidence.json`) remains dated to its original execution. It covers authorized bounded improvement and actual toy input/disposal/ownership behavior. It is separate from the Three.js integration target and never promotes the toy CPU result into Three.js/FPS evidence.

Commands completed: `.venv/bin/python -m unittest discover -s tools/review -v` (12PASS); package/configuration suite (13PASS); actual Codex client checks via `tools/plugin/check_codex_client.py` and `tools/plugin/check_review_skill_client.py`; plugin manifest validator PASS. Setup-page desktop/mobile test passed with unchanged secondary-page layout and the new twelve-skill text; final archive version/hash link is verified above. Full independent issue acceptance is the reviewer's responsibility.
