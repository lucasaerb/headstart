# Issue15 setup page — author evidence

`reuse_plugin` authored the nine-skill0.5.0 setup/download page and restrained controls at1c5557e. `reuse_mcp` completed the third static allowlist, limited local sign-in to the auth adapter's actual numeric loopback host, guarded null probe responses and added desktop/mobile browser coverage. Existing C4 production plugin-page typography, blue/pale-sky colors, white rounded panels and spacing remain unchanged. No landing world, library or game illustration changed. Parent owns package/CI commands; independent reviewer must approve this page and the complete release.

Actual browser command: `HEADSTART_CHROME_CHANNEL=chrome SCREENSHOT_DIR=/tmp/headstart-plugin-setup-captures node tests/plugin-setup-ui.mjs` —PASS on Chrome at1440×1000 and390×844, reduced-motion. The suite seeds a temporary real local catalog, calls the actual Node/Python search service, downloads the actual0.5.0 ZIP, checks keyboard activation, truthful unconfirmed-client state, local sign-in link, fallback explanation, retry controls and horizontal overflow. Service-unavailable and unsupported-version cases are explicitly intercepted fixtures; neither is represented as a real service outage. No credentials or user records are used.

Screenshots: `/tmp/headstart-plugin-setup-captures/{desktop,mobile}-{entry,compatible,unavailable,unsupported,fallback}.png`; exact hashes and retained-style digest in `captures.json`. Author visually inspected full desktop entry and mobile compatible state: text remains legible, action labels wrap naturally, cards align without clipping, and the status explicitly distinguishes service compatibility from plugin installation. The mobile controls stack within the existing panel. This is author inspection, not independent visual acceptance. Actual Codex installation/lookup evidence belongs to the plugin author's separate release checks; the browser probe cannot establish those.
