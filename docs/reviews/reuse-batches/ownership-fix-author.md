# Issue12 repository-control correction — author evidence

Author `reuse_mcp`, following independent reviewer finding. Ownership verification now independently retrieves the claimed public GitHub repository identity/default branch, then resolves that repository's own branch HEAD through the GitHub API. The challenge is fetched only at that resolved immutable commit. A caller-provided commit is merely an optional expected-value check; it cannot select which Git object establishes control. Matching challenge bytes available only at an unrelated fork-network commit fail before raw-content retrieval. Saved evidence includes repository ID, default branch, reference API URL, resolved commit, reference digest, challenge digest and observation time. Control proof still does not grant publication, licenses, creator authority or payout terms.

Transport now uses the existing TLS connection pinned to a resolved global address: only api.github.com and raw.githubusercontent.com, no inherited proxies, credentials, redirects, compressed responses or nonpublic DNS results; bounded API262144bytes/challenge4096bytes. GitHub API version2026-03-10 is explicitly requested. No external repository was modified and no actual repository-control claim was made.

Official references read10 September2026: [Get a repository](https://docs.github.com/en/rest/repos/repos#get-a-repository), [Get a branch](https://docs.github.com/en/rest/branches/branches#get-a-branch). These define repository default_branch and branch commit.sha responses used in the resolver.

Author validation: `.venv/bin/python -m unittest discover -s services/submissions/tests -v` —11PASS. Tests cover valid pinned default-branch proof, matching fork-only commit rejection, commit-only content without API identity, wrong repository identity, malformed SHA, receipt/revision boundaries, public TLS address pinning, private-address rejection, redirects, response size and absent authorization headers. Existing review/rights-freeze tests remain passing. Synthetic upstream fixtures exercise the documented contracts; they are not live GitHub verification evidence. No UI changed; screenshots not applicable to this correction. Independent review remains required.
