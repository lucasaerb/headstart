# Full independent review: #27 and #13 — PASS

Reviewer: `reuse_mcp`. Authors: `reuse_auth` (#27), `reuse_handoff` (#13). Reviewed source at `5de97aa0926fe04de3657c31c14fab88ef5b5a01`; exact relevant file hashes and capture/reference hashes are recorded in `auth-handoff-full-digests.json`, combined source digest `b27a0eb3ae48c256dcacf1f27e81eedbd24e8c2eedda53b118f8f4985f47357d`. This is a full issue review of the requested **localhost** delivery, not only the earlier prerequisite interface gate. The reviewer did not author auth or handoff implementation. The review does not approve this reviewer's MCP implementation, plugin-page additions or ownership-proof correction; those receive separate review by `reuse_handoff`.

## Actual independent checks

- `.venv/bin/python -m unittest discover -s services/auth/tests -v` —8PASS.
- `.venv/bin/python -m unittest discover -s services/handoff/tests -v` —16PASS.
- `node --test tools/dev/auth-handler.test.mjs` —1 real HTTP test PASS.
- `HEADSTART_CHROME_CHANNEL=chrome SCREENSHOT_DIR=/tmp/headstart-full-auth node tests/auth-ui.mjs` —desktop/mobile PASS.
- `HEADSTART_CHROME_CHANNEL=chrome SCREENSHOT_DIR=/tmp/headstart-full-handoff node tests/handoff-ui.mjs` —desktop/mobile PASS, including deliberately delayed handoff script loading.
- `HEADSTART_CHROME_CHANNEL=chrome node docs/reviews/reuse-batches/review-handoff-browser.mjs` —independent desktop/mobile PASS through the real public reviewed-system card → Inspect component → prepare → local preview verification → automatic resumed packet → actual JSON download → logout → blocked Markdown download. This closes the author-suite gap where the handoff action was mounted directly.
- `node docs/reviews/reuse-batches/review-auth-handoff-http.mjs` —independent real HTTP PASS: anonymous public search, crafted verification flag, missing CSRF, browser-cookie-as-bearer, browser-origin bearer, wrong-owner artifacts/bags, no active bag, actual approved MCP credential retrieval, immutable brief revision/preserve constraint, unsupported recipe, logout/child revocation, non-enumerating known/unknown challenge responses, oversized requests, artifact rate limits and private-path rejection.

All identities, databases and preview deliveries were temporary synthetic local fixtures. No live email, production credentials, remote repository execution or target writes occurred. The HTTP reviewer script does not print credentials. Source inspection covered the complete auth store/API/CLI, Node routing/CSRF/credential transport, auth UI, handoff domain/API, scoped detail integration, tests and deployment-independent local setup documentation.

## #27 acceptance

**PASS: public versus protected boundary.** Anonymous catalog metadata is still served. Existing public source/demo and local research collection behavior is unchanged; demo-email collection remains separate from login. Direct protected generation and delivery authorize against server-owned verified account/session state, never a UI boolean. Browser cookies and MCP credentials have distinct kinds; a browser cookie cannot bypass CSRF by being placed in Authorization. Protected artifacts are not static files or public capabilities. Every historical/current bag and packet read rechecks authorization. Private databases, preview storage and service source remain outside static delivery.

**PASS: one-time verification and session lifecycle.** Source and tests establish 256-bit random challenge tokens, hash verifiers, ten-minute expiry, browser binding, five attempts, supersession and transactional consumption. Concurrent replay has exactly one winner. Issue/resend and verification throttles are exercised. Known/unknown addresses return the same response shape/status. Cookie lifetime is bounded; HttpOnly, SameSite=Strict and exact-origin JSON/CSRF policy are tested. Successful verification rotates the prior session; logout revokes it and its approved MCP children. Expired, unverified, forged and revoked sessions reject. Unsupported hosted origins fail closed; the insecure-cookie exception is explicitly localhost-only.

**PASS: intent recovery and privacy.** Return destination is server-fixed `/#games`; an allowlisted action, immutable digest and bounded selection references replace caller-controlled redirects. The brief remains local. Actual browser recovery compares the saved auth intent and canonical bag digest before generation; source eligibility and rights are checked again server-side. Delayed script loading now retains the resume intent until the handoff consumer succeeds. Invalid verification and canceled/failed operations preserve local selections. Tokens appear only in the explicitly requested private local preview, not HTTP bodies or application logs; fragment removal, no-referrer and restricted auth-page CSP reduce disclosure. Verification grants no marketing subscription or local edit permission.

**PASS: local setup and UI.** README/CLI provide development preview and explicit browser-approved local MCP pairing, mode0600 credential file and bounded, revocable access. No tokens/cookies are requested in model prompts. The auth page exposes anonymous, challenge-issued, invalid/expired, verified/sign-out, revoked and unavailable messages with live status announcements, labeled email input and keyboard access. Hosted provider delivery remains intentionally unavailable, as the issue permits.

## #13 acceptance

**PASS: exact, deterministic planning handoff.** The validated request pins one to three component versions and brief revision/constraints; parent project versions, commit/evidence digests, dependency closure, rights, original notices, creators, target assumptions, inspection/validation/rollback requirements and modification/provenance fields remain present. Non-null recipes and unresolved external dependency scopes fail with structured actionable errors; no assembled-recipe claim is invented. The packet explicitly distinguishes source review, actual reuse, optional public lineage and unresolved royalties. No source-code bytes are exported.

**PASS: rights and historical delivery.** Current publication, complete source evidence and applicable asset/data rights are evaluated on generation and delivery. Rights-report freezes, withdrawn versions, missing policy state, unresolved assets, tampered evidence and stale versions fail closed. The HTTP delivery transaction serializes the scope/freeze check with catalog writes. Historical packet content remains immutable and digest-checked; a changed brief creates a new artifact. Current selection is an explicit owner-scoped pointer updated atomically when a packet is prepared, including reselecting an older packet. It is not an arbitrary database row or the anonymous research-game bag.

**PASS: notices and round trip.** Independent tests verify exact Gabriele Cirulli copyright notice retention, record digests, deterministic repeated generation, historical retrieval and lossless JSON/Markdown round trip. A dynamically longer Markdown fence preserves hostile backticks as data. Source code is absent from the metadata packet. The independently exercised real UI presents JSON and Markdown actions in the existing details surface; each click calls the protected service again, so a retained download button fails after logout. Public reference planning remains an explicitly separate fallback.

## Independent visual acceptance — PASS

The user-selected C4 living-world direction and related C3 grid/bag board are the references, not a new provisional direction. I opened both actual boards and compared the new UI captures; their exact paths/digests are in the manifest. These boards do not supply a dedicated email-verification screen, so the auth card is an adaptation using the existing navy type, pale sky background, blue primary action and white rounded panel language. The handoff extends the existing source dialog; it does not replace or restyle the landing world.

Chrome captures cover desktop1440×1000 and mobile390×844, reduced motion. Inspected desktop auth entry/verified and handoff ready, mobile challenge sent/invalid/logout and handoff ready/blocked, plus actual-catalog entry captures. Hierarchy, readable paragraph widths, aligned fields, visible keyboard focus, adequate control sizing and status announcements pass. Mobile download controls stack without overflow or clipped text. The dialog preserves intent after return; blocked delivery gives a clear retry/verification message. No generated game imagery was added or claimed as execution evidence.

One nonblocking refinement: after verification, the account page could show a short “Signed in” label above Sign out; its current sign-out control already exposes the valid session and does not affect recovery or authorization. No required acceptance or design finding remains unresolved.

## Final verdict and limits

**#27 PASS. #13 PASS.** Accepted for the documented localhost preview and metadata-first handoff scope. This does not establish live inbox delivery, hosted auth/user storage, platform source packaging, anonymous three-game website-bag synchronization, target integration, a playable mashup, royalties or deployment readiness. The package/client release and other three issues retain their own independent acceptance gates. No production deployment or issue closure was performed by this reviewer.
