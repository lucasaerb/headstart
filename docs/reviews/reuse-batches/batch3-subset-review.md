# Batch 3 full review — issues 12, 14 and 15

Independent reviewer: `reuse_handoff`. Authors: `reuse_curator`/`reuse_mcp` (#12), `reuse_mcp` (#14), `reuse_plugin` with bounded `reuse_mcp` setup work (#15). The reviewer authored #13 and the auth recovery timing fix, so does **not** approve #13 or #27. Those require `reuse_mcp`'s separate complete review. This subset report plus that distinct report is required before any aggregate batch PASS.

Review began after all five author handoffs, from `5b6fc81`, and includes later referenced fixes. Criteria: full frozen issue bodies in `docs/planning/reuse-batches/issues-snapshot.json` and complete `batch3-checklist.md`; prerequisite records do not substitute for full acceptance.

## Issue 12 — PASS after fixes

Inspected private queue/store, Node authenticated curator wrapper, public submission UI, immutable review history, scoped rights freeze policy, explicit repository-control verifier, and actual outcome of approval. Curator approval records a reviewed proposal revision; it does not silently publish catalog source or alter existing evidence. Scope changes still use the catalog's evidence/rights gates. Owner control never grants publication or clears rights reports.

- `.venv/bin/python -m unittest discover -s services/submissions/tests -v`: **11 passed**, covering input/URL limits, receipt isolation, duplicate proposals, reviewer authorization, required evidence, stale/concurrent updates, immutable history, report/appeal/resolution freeze semantics and repository-specific default-branch proof. See `ownership-fix-review.md` for accepted correction `477af9b`.
- `HEADSTART_CHROME_CHANNEL=chrome SCREENSHOT_DIR=/tmp/headstart-review-batch3-submissions node services/submissions/tests/browser.mjs`: **PASS**, Chrome desktop 1440×1100 and mobile 390×844; real HTTP anonymous queue rejection, cross-origin/malicious URL rejection, submission, private receipt, authorized queue, immutable detail/history, missing-evidence rejection and successful reviewed revision. The test's reports use synthetic local records, not a real maintainer claim.
- First independent browser run exposed a test-selection defect: mobile sometimes selected the previously approved desktop row using `.first()`, so it could not verify the requested error state. Author correction `2180667` binds the exact newly returned submission ID and asserts revision 1; independent rerun passes. It also keeps independent captures separate using `SCREENSHOT_DIR`.

Captured independent states are `evidence/submissions/`: entry, saved, unauthorized, queue, history, review error and report form at both viewports. I visually inspected mobile entry/queue and desktop review error/history. Labels, buttons and receipts wrap without horizontal overflow; the blue primary action, sky background, navy type and quiet white sections preserve the selected C4/C2 visual language. The long private queue is intentionally vertical, with editable fields and historical JSON separated; it does not intrude on the landing world. New curator states have no exact supplied mockup, so the selected library/detail hierarchy is adapted rather than inventing a supposed reference screen. No blocking visual finding remains.

## Issue 14 — PASS after full-review fix

The narrow protocol prerequisite was accepted after the malformed-response crash fix; `mcp-prerequisite.md` preserves both rounds. Independent current rerun `HEADSTART_MCP_PYTHON=/tmp/headstart-mcp-client/bin/python npm run test:mcp`: **8 transport tests + actual official SDK flow PASS**. Running without the documented optional SDK interpreter failed on missing `mcp`; this is an environment prerequisite, not a fabricated passing default run. Public metadata never sends auth, protected requests use owner-bound paired credentials, exact versions/digests are preserved, unknown/offline states fail honestly and no target writes occur.

Full-review blocking validation finding (medium): `LiveCatalog.bag` accepts a digest-consistent but structurally invalid bag, e.g. `{"schemaVersion":1,"selections":[null],"brief":"not a brief","intent":99}`. Independent probe through `call('get_selected_bag', {})` returned success. The protected server subsequently rejects this if preparation is attempted, so this is not an authorization bypass; it is an unsupported service contract represented as a valid selection. Validate exact selection references, strict schema version, brief revision/constraint shape and bounded string intent before returning success, with malformed-bag regression coverage. Author corrections `9a05ff4` and `5de97aa` add the complete checks and a regression covering malformed selection, boolean schema/revision, brief fields, origins, length limits and recovery to a valid request. Independently reran `HEADSTART_MCP_PYTHON=/tmp/headstart-mcp-client/bin/python npm run test:mcp`: **9 transport tests plus actual official SDK flow PASS**. Inspected the fix; the finding is resolved and full #14 verdict is PASS.

## Issue 15 — PASS at final artifact 46964ab

Inspected packager allowlist/deterministic hashes, manifests, all six core skill instructions plus three companion skills, mode-specific version arguments, local configure helper, progressive references, private credential-path configuration, local cache versioning, rollback and restrained setup UI. The helper never reads credential contents, does not claim connection, validates the intended plugin layout/version and restores original config on an observed failed replacement. Unsupported workspace access remains an actionable local plan/permission boundary rather than escalation or upload.

- `.venv/bin/python -m unittest discover -s tools/plugin -v`: **13 passed**, including deterministic relocated ZIPs, secret/cache exclusion, symlink/size failures, atomic archive behavior, companion boundaries, explicit local/offline configuration and rollback after a failed replacement.
- `HEADSTART_CHROME_CHANNEL=chrome node tests/plugin-setup-ui.mjs`: **PASS**, actual local catalog response and actual ZIP download, keyboard action, truthful manual/unconfirmed-client state, fallback, mobile overflow; unavailable/unsupported responses are explicitly intercepted fixtures.
- Independent captures retained in `evidence/plugin-setup/`. Inspected desktop full entry and mobile compatible state. The existing C4-derived plugin page typography, pale blue surfaces and blue actions are preserved. Mobile buttons stack/wrap, status remains readable, and HTTP compatibility is explicitly distinct from installed/connected acknowledgment.

Final source `46964ab` and author evidence are now complete. Independently rebuilt the archive with `.venv/bin/python tools/plugin/build_distribution.py --output /tmp/headstart-review-final-0.5.0.zip`: **35 files**, SHA-256 `af4f44b9367e95f44900fc5e932f18f9a3ee7ec46a4609c9dd602762dc4301fd`, exactly matching the website download and displayed checksum. Independently verified all 34 distribution-manifest payload hashes and that all **13 installed runtime/skill hashes** in the actual client receipt match both the current source and final ZIP, including the stricter bag validator. `.venv/bin/python -m unittest discover -s HeadStart-Starter-Package/headstart-plugin/tests -v`: **16 passed**, protecting the default offline/snapshot mode.

Inspected `tools/plugin/check_codex_client.py`, `docs/reviews/plugin-core/codex-client.json`, `author.md` and `cleanup.json`. The actual Codex CLI 0.154.0 app-server receipt shows a fresh distinct test installation with nine enabled skills and compatible connected server version; actual tool calls establish anonymous public lookup, unpaired denial, exact detail, a selection posted through the real browser HTTP route with CSRF, automatic current bag and matching protected handoff, missing-version error, parent revocation, stopped service, explicit offline reconfiguration and installed 0.3.0 rollback. Local configuration cache versions differ intentionally by origin/path configuration; runtime and skill bytes are checked equal. This reviewer independently audited the receipt, assertions and exact artifact correspondence rather than altering the user's existing personal plugin installation again. The official SDK path was independently rerun as described under #14.

The client evidence is a real app-server/tool check with synthetic local identity, not an LLM gameplay run. Only test marketplace/server-key adaptation differs; the existing personal 0.3.0 installation was not targeted. Cleanup explicitly notes one discovery-manifest difference versus the archived version and lack of a complete pretest byte baseline; this review does not invent a byte-for-byte preservation proof. Denied workspace access is handled by the packaged instructions and helper boundary, with no hidden target-write or permission-escalation service. No blocking #15 finding remains.

## Design references and remaining gate

Selected originals: C4 `81f0dd99b3891c0ecd33605544e99617c5eaf8a803baf737c4b1597e44f0a8f9`, C2 `bad898855647f1782220aa886e75247a8d5fd396bc2476ace09ae33176b97d6f`, C1 `17253dec26774f5169adb0a5f060cde0ff85c31b3f0a27a1119c61b33cef5dad`; paths and selection context in `docs/design/C4-FINAL-STORYBOARD.md`. The operations/setup surfaces use the existing site tokens; C4 hero artwork/layout was not redesigned. Independent capture hashes are in `evidence/subset-capture-digests.json`.

Final subset verdict: **PASS for #12, #14 and #15**, including their applicable desktop/mobile design criteria and the documented corrections. Exact reviewed implementation/artifact hashes are in `batch3-subset-digests.json` (source `46964ab`). I also independently confirmed no changes versus base `84795b9` in `styles.css`, `app.js` or hero assets.

Aggregate five-issue verdict is still pending the distinct full #27/#13 reviewer report. This subset PASS does not authorize production deployment, claim a three-game playable remix or establish royalty services.
