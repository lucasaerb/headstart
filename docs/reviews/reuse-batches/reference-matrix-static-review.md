# #17 matrix — independent static checkpoint

Reviewer `reuse_handoff`. **Review in progress; no full or prerequisite PASS yet.** Inspected source checkpoint `7361abb`, local promotion `0fc3e92` and workflow recommendation continuity `2b397e6`. Final0.7 author evidence and independent runtime reproduction remain pending; earlier0.6/CI runs are supplementary evidence, not final-version acceptance.

Independent light contract suite: twenty tests PASS (matrix authorization/closure/rollback/mode/recipe guards, promotion denial and core workflow checks). No Docker work overlapped the author's final run.

Inspected all ten source-consuming branches and corresponding external oracles. Rounded geometry checks extents/corner exclusion/blended normals; parametric geometry checks independent sample positions/UV endpoints; line geometry checks exact endpoints; convex geometry verifies extreme/interior points, face orientation and volume with complete ConvexHull source closure. Transformed normal/tangent helpers compare world endpoints; Capsule and OBB distinguish overlap/separation and boundary/rotation behavior through visible target state. Both noise modules change actual mesh coordinates with deterministic repeated input, changed-input variation and connected index traversal. All rows retain the existing renderer/camera/input/loop/terrain owners, feature teardown and whole-target repeated lifecycle checks.

The matrix explicitly excludes camera/controller, collision-response, physics-engine and R3F support. The added Capsule/OBB functions are geometry queries, not a new physics world. The fixture's landmark slot supplies a concrete use for each capability. No-op and disposal mutants are separate from successful evidence; the final independent run must verify each rejects through its actual oracle, not a browser startup failure.

Source maps, exact engine peer and source notices are pinned before copying; Python materialization avoids Git checkout filters and preserves file modes. Applied state binds bytes/modes; rollback protects later edits. Attestations record source, target base and integrated state, recipe, plugin, image and individual log digests. Catalog promotion is an explicit separate operation requiring a different review identity and exact validation/attestation/recipe acceptance, not part of ordinary seeding. Target/recipe redistribution rights remain unresolved and candidate rather than borrowed from the tested source component.

Suggested promotion hardening sent to the author: require the exact four nested result phases and validate each report's PASS/row/phase binding directly before deriving passed attestation checks. Normal runtime validation already refuses failed phases, but the promotion boundary should also reject malformed retained structures. This remains under review with final promotion tests.
