# Reuse-batch prerequisite reviews

## #27 local authentication prerequisite — PASS

Independent reviewer: `reuse_reviewer`; author: `reuse_auth`. Reviewed auth commits `cc282d9` and `f736cf2` on 10 September 2026. Exact reviewed auth file hashes are in `auth-prerequisite-digests.json`; sorted compact JSON digest `c43fcaad87f53cc3a04603728f063a1b08f667a545485b0013101498999eaa12`.

This **narrow PASS** permits #13 to consume the local server authorization interface. It is **not full #27 acceptance**, hosted inbox verification, source-handoff acceptance or actual service-connected MCP acceptance. Those require the combined five-issue review and dependent end-to-end checks. No live provider or production credentials were used.

Actual code inspected: auth store/API/CLI, Node auth and handoff transport wrapper, server boundary, auth UI and tests. Independently ran eight Python tests and the real HTTP Node test, all passing. Independently ran the Chrome desktop (1440×1000) and mobile (390×844) browser suite: verification, invalid link, retained bag, keyboard focus, logout and overflow checks passed. Independent screenshots are `/tmp/headstart-review-auth/`; the committed author captures remain `docs/reviews/27-verified-email/evidence/`. Inspected the desktop entry and mobile verified captures: restrained pale surface, navy headings, blue actions and readable form without horizontal overflow. Full storyboard comparison and all integration states remain part of batch review.

Additional independent probes exercised actual CLI pairing-file creation, 0600 permissions, refusal to overwrite, no token in printed output, inactive credential rejection, explicit pairing, browser/MCP credential-kind rejection, session rotation revoking the child MCP credential, and rejection of hosted/HTTPS/path/userinfo auth origins. All passed. Tests also establish atomic one-winner replay handling, binding/expiry/attempt limits, account/client throttles, origin/CSRF rejection, cookie flags, server-side forged/expired/revoked session rejection and private database HTTP 404.

No blocking prerequisite finding. Local preview is explicitly an operator development exception, not proof of remote inbox ownership. Protected services must still authorize every generation/delivery, enforce `credentialKind`, revalidate scope/rights and never regard a demo-email preference or anonymous planning prompt as authorization. The handoff wrapper itself cannot establish that until #13's actual routes and artifacts pass review.

Nonblocking full-review follow-up: the already-verified account screen still emphasizes verification and shows the email form; a clear verified-state heading would better distinguish success from the entry state. This does not invalidate server authentication or gate prerequisite use.

## Preliminary #12 finding for full batch review

**CHANGES REQUESTED, high: repository-control proof.** `services/submissions/store.py:verify_ownership` retrieves an ownership challenge at a caller-supplied commit and labels repository control verified. A commit object's availability does not establish write control of the claimed repository's own references. GitHub's [fork reference](https://docs.github.com/en/pull-requests/reference/forks) explains that forks share Git data within their repository network; this is the basis for the reviewer's inference that commit-only proof is insufficient. Resolve a trusted current reference of the claimed repository independently, pin its commit, and verify the per-submission challenge in that tree. Add rejection evidence for a matching challenge at an unrelated/fork commit. This was found during preliminary code inspection; full #12 acceptance is still pending, and no attacker repository was created or modified.

## #13 protected handoff/MCP service prerequisite — PASS

Independent reviewer: `reuse_reviewer`; author: `reuse_handoff`. Reviewed domain source from `5c7ee1b` plus current-bag/route/permission changes in the working tree at parent `25e50dd`. Exact prerequisite source hashes: `handoff-prerequisite-digests.json`; compact sorted JSON digest `17e090cf4541cb88f6c45860969d0d34acc2ffbb116147e088dbe5704d8a3826`.

This **narrow PASS** permits #14 to consume the authenticated handoff, immutable bag and account-current-bag HTTP interfaces. It is not full #13 acceptance or proof of a connected MCP client. Combined batch review remains required. The independently discovered #12 ownership-proof finding does not undermine this prerequisite: ownership proof never grants catalog publication or releases rights freezes; export permission consumes exact curator states independently.

Inspected exact component/project versions, canonical deterministic record/payload digests, notice and creator preservation, target/brief/intent data, unsupported recipe/dependency errors, attachment rights, current evidence validation, owner isolation and per-delivery authentication. Fifteen independent Python handoff tests pass, including immutable historical artifacts, missing rights-policy schema, scoped report freezes, tombstones, stale versions, asset rights, evidence tampering, current-bag owner/reselection and withdrawal handling. Delivery starts a catalog write transaction before scope/freeze validation; current-bag selection updates atomically with successful prepare and does not mutate historical artifacts.

Additional independent real HTTP probes passed: anonymous generation/download/current-bag rejection; missing browser CSRF rejection; browser session reused as bearer rejection; browser-origin bearer rejection; approved MCP credential generation/download/immutable bag access; current-bag initially absent; Alice's two selections and reselecting the first artifact; Bob unable to read Alice's current selection; and withdrawn scope blocking current-bag retrieval. These used temporary local databases, synthetic identities and no production credentials.

Browser inspection completed the actual local verify→resume→JSON download→logout→blocked download sequence at desktop 1440×1000 and mobile 390×844. The initial test used an implicit context and could not open its logout tab; author corrected this, and independent rerun passed. Inspected desktop ready, mobile ready and mobile blocked captures in `/tmp/headstart-review-handoff/`: C4-style light dialog, readable navy text, blue actions, preserved entered intent and no horizontal overflow. Full visual acceptance, discoverability through the real catalog detail, and a subsequently identified delayed-script resume race regression remain for the full batch review. They do not affect the domain/HTTP contract used by #14.

No blocking service prerequisite findings. Metadata packets are not source delivery, tested integration, royalty evidence or permission to modify a target. `GET /v1/bags/current` selects the account's last explicitly prepared eligible bag; anonymous local collections are not silently synchronized.

Follow-up at `0eef3a65d8197a5ba1ef6f900e4ac74aab896db8`: independently reran all **16 handoff Python tests**, including fresh catalog initialization without ad hoc rights-policy setup, and the desktop/mobile browser suite with a deliberate 150ms delay to `handoff.js`; both passed. Inspected the initialization addition and auth recovery patch: dispatch waits for deferred consumers and retains recovery state until successful handoff acknowledgment. This resolves the two prerequisite follow-up gaps noted above. Full five-issue acceptance remains pending.
