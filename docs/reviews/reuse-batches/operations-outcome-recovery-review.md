# Issue20 reviewed outcomes and recovery — narrow review

**PASS for these interfaces only**, independent reviewer `reuse_handoff`, reviewed source92d009a (events), eee9a10 (catalog initialization/recovery),8c64e02 (local monitor/CLI). Full issue20 UI and actual browser worker acceptance remain open.

I inspected the exact acceptance registry and independently ran all25 current operations tests plus31 catalog tests. The adapter reads the existing independently accepted e937b30 matrix; it does not execute, invent or reapprove it. Both acceptance document and receipt are pinned by raw-byte digest, then all ten source/target/recipe/phase/attestation/log/capture bindings are checked before aggregate insertion. Replay is deduplicated; oversized/missing/symlink/tampered material fails atomically. Only opaque evidence keys, public acceptance digest and ingestion time persist. Explicit enabled local ingestion is required; client events cannot assert verified reuse. The displayed scope is ten authored reference integrations, explicitly not user adoption. Thirty-day retention and distinct self-reports remain intact. A future reference bundle requires a separately reviewed registry update.

The catalog constructor now avoids schema writes for an already complete known schema. Its crash test opens a new actual public search connection while a separate process holds a valid uncommitted immutable-version update, terminates that writer via os._exit(23), then verifies old public results survive and a later valid commit is visible. This reproduces the relevant availability boundary rather than relying on a mocked failure. A missing immutable trigger still invokes repair. All31 catalog behavior/rights/transaction tests pass.

The local source monitor records immutable source/license snapshots and schedules weekly rechecks with bounded retries; changed snapshots create unresolved candidates, not promoted tested versions. CLI activation is explicit local work. Dated interactive reports are stored separately from reachability and require a matching registered target/plan. These inspected storage interfaces do not alone establish successful safe browser execution or the required operations/public stale UI; those need the remaining full issue20 review.
