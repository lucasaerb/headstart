# Batch4 wrap-up critical review

**Verdict: CHANGES REQUESTED — full #16–#20 acceptance is not established.** Reviewer `reuse_handoff`, independent of these five implementations. This records the wrap-up checkpoint after `bbdda3b` and the final #17 visual source update `2ea7a4d`; the working tree also contains unfinished, uncommitted UI/client changes. Do not mark all five complete or represent pending work as deployed.

| Issue | Established evidence | Remaining acceptance blocker |
| --- | --- | --- |
| #16 | Prerequisite PASS at5458375 after host-filter, evidence and file-mode fixes; actual isolated desktop/mobile reference, negative mutants and drift/rollback checks. Current recommendation-context contract tests pass. | Final source now includes0.7/context changes; retained earlier runtime has a different recipe digest. Complete exact final runtime/evidence acceptance in the combined batch before claiming final tested status. |
| #17 | Static ten-capability oracle/closure/lifecycle review, twenty light tests; promotion guard hardening197585a independently passed two tests. Prior author/CI ten-positive and mutant runs are supplementary. | Author committed2ea7a4d visual changes after finding occlusion/edge-on geometry. Exact final40 phases and20 mutants are running; independent reproduction and all final captures have not yet passed. |
| #18 | Three review rubrics, real first-party fixture execution/captures, raw evidence checks, source cross-binding fix and schema2 continuity tests. Rebuilt0.7 archive exactly, all44 manifest payload hashes and nineteen installed runtime/skill hashes per install match. | Retained actual three-skill invocation is0.6; final0.7 invocation/image-view receipts and the final client artifact are being refreshed. Complete final proof and source freeze before full issue acceptance. |
| #19 | Three inactive source-pinned planning templates, actual curated-catalog projection, eleven rules/context/handoff tests, server-derived rationale and scope freezes. | `site/dist/recipes.html`, `recipes.css`, `services/recommendations/api.py` and handler are untracked work in progress. No registered recommendation route or completed desktop/mobile query→recommendation→verified handoff flow was reviewed. This is not yet published site behavior. |
| #20 | Sixteen inactive foundation tests: consent/retention/deletion, distinct reachability categories/leases/backoff, immutable change candidates and serial retry handling. | Operations UI/CLI and public stale-state workflows are not complete or independently captured. Actual interactive observations, reviewed reuse bridge and interrupted-index recovery acceptance are not established. Concurrent change dispatch still duplicates corrections. |

Concrete #20 findings:

- **P2: duplicate queue corrections under concurrency**, `services/operations/changes.py:60`–67. Two real SQLite connections gated at Queue.all each submit the same candidate as different queue IDs. Recorded reproduction is in `operations-foundation-review.md`; author acknowledged the pending fix. A transactional claim/lease or idempotent queue boundary and concurrent crash/retry regression are required before activation.
- **P1 completion gap: reviewed reuse is unavailable**, `services/operations/events.py:91`–92 returns null and `reviewed_evidence_adapter_not_activated`. This is honest, but does not satisfy the required reviewed-outcome metric. Interaction events and self-reports cannot substitute.
- **P1 completion gap: interactive health is not integrated**, `services/operations/health.py:165` always returns unchecked with no observed date. The separate browser broker is unfinished worker infrastructure, not accepted dated gameplay evidence or its scheduled/operations view.
- **P1 completion gap: required active views and job recovery evidence are absent.** `tools/dev/api-routes.mjs` has no completed recommendation/operations activation; there is no accepted desktop/mobile operations/public-stale flow or interrupted index rebuild evidence for this batch.

These are acceptance and implementation limits, not a claim that the already reviewed C4 site regressed. Its existing art direction remains selected. Completed narrow reviews are retained in `local-integration-prerequisite.md`, `reference-matrix-static-review.md`, `expert-review-slice.md`, `recommendation-planning-slice.md` and `operations-foundation-review.md`. Final matrix/client evidence can resolve those particular pending rows, but cannot waive the unimplemented #19/#20 criteria. Any narrower merge must state that scope explicitly and keep incomplete issues open.
