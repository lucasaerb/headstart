# #20 inactive operations foundations — early independent critique

Reviewer `reuse_handoff`; author `reuse_plugin`. Reviewed the events, health and source-change foundations from checkpoints `8ca188f`, `f52fafc`, `82b88bd`, `36dec76`, plus current inactive wiring. **No full issue PASS**: actual interactive/operations UI, reviewed outcome bridge and final activation remain pending. No Docker/browser workload was started during #17's exclusive runtime work.

Independent `.venv/bin/python -m unittest discover -s services/operations/tests -v`: sixteen PASS. Inspected fixed event vocabulary, strict opt-in booleans, public catalog-ID allowlist, HMACed deletion token, deduplication/conflict handling, thirty-day pruning and deletion. Unknown/private property fields cannot be persisted; verified reuse is a distinct unavailable adapter, not a client assertion. The final public client still needs actual consent, disabled-mode and deletion workflow evidence.

Reachability stores fixed dated categories and cannot create interactive success. Public HTTPS URL validation, all-address DNS checks, pinned TLS, redirect refusal and disposable deadline worker bound the network probe. Per-target leases and a transactionally rechecked host reservation separate concurrent workers; the process and category tests exercise unsafe destinations and expiration. A reachable HEAD result explicitly leaves featured interactive checks unchecked. Proposed cadence and host backoff remain distinct from deployed scheduling.

Source/license changes preserve previous pins and create immutable candidate payloads with suggested readiness, review-required rights and null verification. They do not overwrite published source records or borrow tested evidence. Retry exhaustion and crash-after-submit recovery work serially.

**P2 activation blocker — concurrent outbox dispatch duplicates corrections.** Independently used two real SQLite connections and the actual Queue, gating each `Queue.all()` response after both read an empty queue. Both dispatchers submitted the same source candidate and returned different queue IDs; the queue contained two corrections. The current check-then-submit is not an atomic idempotency boundary. Add a transactional claim/lease or unique queue idempotency key, retaining crash recovery, failed-attempt limits and manual retry. The README already describes concurrent exclusion as pending; this finding must be resolved before workers/CLI can activate, not concealed by only testing serialized dispatch.

Before final acceptance, verify new snapshots' actual pinned bytes and queue-review usability, browser resource-broker bounds, immutable dated interactive results, current source/license changes through the real queue, privacy-preserving reviewed outcome evidence, interrupted index rebuild behavior, and desktop/mobile operations/public stale states. These are pending deliverables rather than successes inferred from the foundation tests.
